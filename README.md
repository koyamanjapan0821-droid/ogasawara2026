# 小笠原2026 PWA V4 Drag

## 更新内容
- キャラクターを指でドラッグ移動可能にしました。
- ドラッグ後は30秒間ランダム移動を停止し、その後ランダム移動を再開します。
- 既存のランダム画像切替・タップ台詞は維持しています。
- 参照画像は既存アップロード済みの `assets/characters/` 内ファイルのみです。

## アップロード
GitHub Pages のリポジトリ直下へ中身をアップロードしてください。
`assets/characters/` の画像ファイルは既にGitHub側にあるものをそのまま使います。
